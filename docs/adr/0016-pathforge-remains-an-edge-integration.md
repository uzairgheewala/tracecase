# ADR 0016 — Pathforge remains an edge integration

Tracecase core packages do not import Pathforge. The isolated integration package may bind Pathforge events to public Tracecase contracts and may add namespaced extension schemas and domain analyzers.
