[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "tracecase-collectors"
version = "0.3.0"
description = "Source adapter and collection framework for Tracecase"
requires-python = ">=3.11"
dependencies = [
  "pydantic>=2.8,<3",
  "tracecase-model>=0.3.0",
]

[tool.setuptools.packages.find]
where = ["src"]
