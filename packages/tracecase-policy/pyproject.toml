[project]
name = "tracecase-policy"
version = "0.5.0"
requires-python = ">=3.11"
dependencies = [
  "pydantic>=2.8,<3",
  "tracecase-model",
  "tracecase-bundle",
  "tracecase-graph",
  "tracecase-invariants",
  "tracecase-analyzers",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
